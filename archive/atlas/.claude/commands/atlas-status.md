Report the current ATLAS repository state. Run `git status` and
`git log --oneline -5`, then run `python -m pytest -q` with the project venv
(`.venv\Scripts\python.exe` on Windows). Summarize: branch,
modified/untracked files, test pass/fail counts, and whether dev mode is
healthy (`python -m atlas.app.main --run 1`). Do not modify any files.
