Run the ATLAS test suite: `python -m pytest -q` using the project venv.
If anything fails, show the failing test names and the relevant assertion
output, inspect the involved source files, and propose (do not apply) a
minimal fix. Never delete or skip existing tests to make the suite green.
